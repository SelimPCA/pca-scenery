set_up:
  - reset_db
  - create_testuser:
      !common-item
      ID: TESTUSER
      is_active: False

case:
  credentials:
    !common-item 
    ID: CREDENTIALS
    password: wrong_password

scene: 
  method: POST
  url: app:login
  data: !case credentials
  directives:
    # Account is inactive, 
    # so for now even if password is wrong : send email
    - status_code: 302
